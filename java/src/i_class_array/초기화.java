package i_class_array;

/**

 1. 변수 초기화
    int a;  // 변수 선언(메모리 확보)
    a = 10; // 값 지정

 2. 배열 초기화
    int[] a;    // 변수 선언
    a = new int[3]; // 객체 생성(메모리 확보)
    a[0] = 10;  // 값 지정
    a[1] = 20;
    a[2] = 30;

    int a = new int[]   // 초기화

 3. 클래스 초기화
    Student s;  // 변수 선언
    s = new Student();  // 객체 생성(메모리 확보)
    s.name = "홍길동"; // 값 지정(private가 아니라면)
    s.kor = 100;

    Student s = new Student("홍길동", 100);    // 클래스 초기화

 */

public class 초기화 {
}
